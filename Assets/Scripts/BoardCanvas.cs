using UnityEngine;
using System.Collections;

public class BoardCanvas : MonoBehaviour {

    // Use this for initialization
    void Start () {
        /*string imagePath = @"file://" + Application.dataPath + "/../../" + "GamePacks/D2E/img/DJ01_CoreSet.png";

        WWW www = new WWW(imagePath);
        Texture2D newTex = new Texture2D(256, 256, TextureFormat.DXT5, false);
        www.LoadImageIntoTexture(newTex);
        Debug.Log("Read image from " + imagePath);

        SpriteRenderer tmpSprite = gameObject.AddComponent<SpriteRenderer>();
        tmpSprite.sprite = Sprite.Create(newTex, new Rect(0, 0, 1000, 1000), Vector2.zero, 105);*/
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
